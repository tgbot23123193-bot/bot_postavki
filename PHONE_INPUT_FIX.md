# ✅ Исправление ввода российских номеров

## 🎯 Проблема

При вводе российского номера телефона (например, 79618500085) браузер начинал вводить номер с цифры "7", хотя WB уже автоматически подставляет "+7" в поле ввода. В результате получалось "+7" + "79618500085" = некорректный номер.

## 🔍 Причина

В коде было 3 попытки ввода номера:
1. **Попытка 1:** Вводила `clean_number` (9618500085) ✅ правильно
2. **Попытка 2:** Вводила полный `phone` (79618500085) ❌ неправильно  
3. **Попытка 3:** Включала варианты с "7" в начале ❌ неправильно

## ✅ Решение

**Файл:** `wb_bot/app/services/browser_automation.py`

### Изменение 1: Попытка 2 (строка 2429)

**Было:**
```python
logger.info(f"📱 Попытка 2: Ввод полного номера '{phone}'")
await self._human_type(phone_input, phone)  # Вводило 79618500085
```

**Стало:**
```python
logger.info(f"📱 Попытка 2: Повторный ввод чистого номера '{clean_number}'")
await self._human_type(phone_input, clean_number)  # Вводит 9618500085
```

### Изменение 2: Попытка 3 (строка 2473)

**Было:**
```python
if country_code == '+7':
    variants = [
        clean_number,  # 9001234567
        f"8{clean_number[1:]}",  # 89001234567 
        f"7{clean_number}",  # 79001234567 ❌
        f"+7{clean_number}"  # +79001234567 ❌
    ]
```

**Стало:**
```python
if country_code == '+7':
    variants = [
        clean_number,  # 9001234567 ✅
        clean_number.replace(clean_number[0], '8', 1) if clean_number.startswith('9') else clean_number,  # 8001234567 ✅
    ]
```

## 📝 Как это работает теперь

### Пример 1: Номер начинается с 7

**Ввод пользователя:** `79618500085`

**Парсинг:**
- `country_code` = `'+7'`
- `clean_number` = `'9618500085'` (убрана первая 7)
- `country_name` = `'Россия/Казахстан'`

**Ввод в поле WB:**
- Попытка 1: `9618500085` ✅
- Попытка 2: `9618500085` ✅ (было 79618500085)
- Попытка 3: `9618500085` или `8618500085` ✅

### Пример 2: Номер начинается с 8

**Ввод пользователя:** `89618500085`

**Парсинг:**
- `country_code` = `'+7'`
- `clean_number` = `'9618500085'` (8 заменена на 9, убран код)
- `country_name` = `'Россия/Казахстан'`

**Ввод в поле WB:**
- Попытка 1: `9618500085` ✅
- Попытка 2: `9618500085` ✅
- Попытка 3: `9618500085` или `8618500085` ✅

### Пример 3: Номер начинается с 9

**Ввод пользователя:** `9618500085`

**Парсинг:**
- `country_code` = `'+7'`
- `clean_number` = `'9618500085'`
- `country_name` = `'Россия/Казахстан'`

**Ввод в поле WB:**
- Попытка 1: `9618500085` ✅
- Попытка 2: `9618500085` ✅
- Попытка 3: `9618500085` или `8618500085` ✅

## 🚀 Применение изменений

Чтобы изменения вступили в силу, нужно перезапустить бота:

```powershell
# Остановить все процессы Python
Get-Process | Where-Object {$_.ProcessName -eq "python"} | Stop-Process -Force

# Запустить бота
cd wb_bot
python -m app.main
```

## ✅ Результат

Теперь при вводе российского номера телефона:
- ✅ В поле WB вводится **только номер БЕЗ кода страны** (9...)
- ✅ WB сам подставляет "+7" перед номером
- ✅ Получается корректный формат: "+7 961 850-00-85"
- ✅ Работает для всех форматов ввода (7..., 8..., 9..., +7...)

---

**Проблема исправлена! Российские номера теперь вводятся правильно.** 🎉



