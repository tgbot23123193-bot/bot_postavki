# ✅ Автомониторинг с фильтрацией по API

## 🎯 Что было реализовано

Полностью переработана кнопка **"✅ Начать мониторинг"** - теперь она делает реальный запрос к API, фильтрует склады по выбранным параметрам и показывает результаты с пагинацией.

## 📝 Изменения

**Файл:** `wb_bot/app/bot/handlers/browser_booking.py`

### Новая логика "Начать мониторинг"

```python
@router.callback_query(F.data == "browser_start_monitor")
async def browser_start_monitor(callback: CallbackQuery, state: FSMContext):
    """Запуск автоматического мониторинга с фильтрацией по API."""
```

### Этапы работы:

#### 1. Получение параметров

```python
# Из FSMContext state:
period = data.get('monitoring_period', 'any')  # 7, 14, 30, any
max_coef = data.get('max_coefficient', 'any')  # 1.0, 1.5, 2.0, 2.5, 3.0, any
```

#### 2. Запрос к API

```python
async with WBSuppliesAPIClient(api_keys[0]) as api_client:
    # Получаем все склады
    warehouses = await api_client.get_warehouses()
    
    # Берем топ-30 складов
    top_warehouses = warehouses[:30]
    warehouse_ids = [wh.get('id') for wh in top_warehouses]
    
    # Получаем коэффициенты приёмки
    all_slots = await api_client.get_acceptance_coefficients(warehouse_ids)
```

#### 3. Фильтрация по периоду

```python
if period != 'any':
    days = int(period)
    end_date = datetime.now() + timedelta(days=days)
    
    filtered_slots = []
    for slot in all_slots:
        slot_date = datetime.strptime(slot.get("date").split("T")[0], "%Y-%m-%d")
        if slot_date <= end_date:
            filtered_slots.append(slot)
    
    all_slots = filtered_slots
```

**Примеры фильтрации:**
- `period = "7"` → слоты на ближайшие 7 дней
- `period = "14"` → слоты на ближайшие 14 дней
- `period = "30"` → слоты на ближайшие 30 дней
- `period = "any"` → все слоты

#### 4. Фильтрация по коэффициенту

```python
if max_coef != 'any':
    max_coefficient = float(max_coef)
    filtered_slots = [
        slot for slot in all_slots 
        if slot.get("coefficient", 999) <= max_coefficient
    ]
    all_slots = filtered_slots
```

**Примеры фильтрации:**
- `max_coef = "1.0"` → только слоты с коэф. ≤ 1.0
- `max_coef = "1.5"` → только слоты с коэф. ≤ 1.5
- `max_coef = "2.0"` → только слоты с коэф. ≤ 2.0
- `max_coef = "any"` → все слоты

#### 5. Группировка по складам

```python
slots_by_warehouse = {}
for slot in all_slots:
    wh_id = slot.get("warehouseID")
    if wh_id not in slots_by_warehouse:
        slots_by_warehouse[wh_id] = []
    slots_by_warehouse[wh_id].append(slot)
```

#### 6. Отображение результатов с пагинацией

```python
await show_monitoring_results_page(loading_msg, user_id, 0, period, max_coef)
```

## 📊 Отображение результатов

### Функция показа результатов

```python
async def show_monitoring_results_page(message, user_id: int, page: int, period: str, max_coef: str):
    """Отображает страницу отфильтрованных результатов мониторинга."""
```

### Формат вывода (если найдены слоты):

```
🎯 Результаты мониторинга

📅 Период: 7 дней
📊 Макс. коэффициент: 1.5

✅ Найдено слотов: 42
🏬 Складов: 8

📄 Страница 1 из 2

🏬 Коледино
   🆔 ID: 206348
   🎯 Слотов: 12 шт.
   📊 Мин. коэф.: 1.2
   📅 2025-10-05 (1.2), 2025-10-06 (1.3), 2025-10-07 (1.5)

🏬 Подольск
   🆔 ID: 117986
   🎯 Слотов: 8 шт.
   📊 Мин. коэф.: 1.0
   📅 2025-10-04 (1.0), 2025-10-05 (1.1), 2025-10-06 (1.2)

... (еще 3 склада)

💡 Нажмите на склад чтобы забронировать слот

[⬅️ Пред.] [След. ➡️]
[🔄 Обновить поиск]
[⚙️ Изменить параметры]
[⬅️ Главное меню]
```

### Формат вывода (если НЕТ слотов):

```
🎯 Результаты мониторинга

📅 Период: 7 дней
📊 Макс. коэффициент: 1.0

❌ Подходящих слотов не найдено

По выбранным параметрам нет доступных слотов.

Попробуйте:
• Увеличить период поиска
• Увеличить макс. коэффициент
• Запустить мониторинг позже

[🔄 Обновить поиск]
[⚙️ Изменить параметры]
[⬅️ Главное меню]
```

## 🔄 Навигация и управление

### Пагинация

```python
@router.callback_query(F.data.startswith("monitor_page:"))
async def monitoring_results_page_handler(callback: CallbackQuery):
```

- ⬅️ **Пред.** - предыдущая страница результатов
- **След. ➡️** - следующая страница результатов
- По 5 складов на страницу

### Кнопки управления

1. **🔄 Обновить поиск** (`browser_start_monitor`)
   - Повторный запрос к API с теми же параметрами
   - Обновляет данные

2. **⚙️ Изменить параметры** (`browser_auto_monitor`)
   - Возврат в меню настроек
   - Можно изменить период и коэффициент

3. **⬅️ Главное меню** (`browser_menu`)
   - Возврат в главное меню браузера

## 📋 Полный сценарий использования

### Шаг 1: Выбор периода

```
Пользователь → 🤖 Автомониторинг → 📅 Выбрать период → 📆 Ближайшие 7 дней

Сохранено в state:
monitoring_period = "7"
```

### Шаг 2: Выбор коэффициента

```
Пользователь → 📊 Макс. коэффициент → 1.5

Сохранено в state:
max_coefficient = "1.5"
```

### Шаг 3: Запуск мониторинга

```
Пользователь → ✅ Начать мониторинг

Бот:
1. ✅ Проверяет API ключ
2. 🔍 Показывает "Запускаю мониторинг..."
3. 🏬 Получает список складов (30 штук)
4. 📊 Получает коэффициенты приёмки
5. 📅 Фильтрует по периоду (≤ 7 дней)
6. 📊 Фильтрует по коэффициенту (≤ 1.5)
7. 🏬 Группирует по складам
8. 📄 Показывает результаты (страница 1)
```

### Шаг 4: Просмотр результатов

```
Результат:
- Найдено 42 слота на 8 складах
- Показывается 5 складов на странице
- Для каждого склада:
  • Название
  • ID
  • Количество подходящих слотов
  • Минимальный коэффициент
  • Ближайшие 3 даты с коэффициентами
```

### Шаг 5: Навигация

```
Пользователь может:
- Листать страницы (⬅️ Пред. / След. ➡️)
- Обновить данные (🔄 Обновить поиск)
- Изменить параметры (⚙️ Изменить параметры)
- Вернуться в меню (⬅️ Главное меню)
```

## 🔍 Примеры фильтрации

### Пример 1: Строгая фильтрация

**Параметры:**
- Период: 7 дней
- Макс. коэффициент: 1.0

**Результат:**
```
До фильтрации: 150 слотов
После фильтрации по периоду: 80 слотов
После фильтрации по коэф.: 12 слотов
Складов: 3
```

### Пример 2: Средняя фильтрация

**Параметры:**
- Период: 14 дней
- Макс. коэффициент: 2.0

**Результат:**
```
До фильтрации: 150 слотов
После фильтрации по периоду: 120 слотов
После фильтрации по коэф.: 65 слотов
Складов: 12
```

### Пример 3: Без фильтрации

**Параметры:**
- Период: любая дата
- Макс. коэффициент: любой

**Результат:**
```
До фильтрации: 150 слотов
После фильтрации: 150 слотов (без изменений)
Складов: 20
```

## 📊 Данные из API

### Структура слота

```json
{
  "warehouseID": 206348,
  "date": "2025-10-05T00:00:00Z",
  "coefficient": 1.5,
  "warehouseName": "Коледино"
}
```

### Логирование

```
🤖 Запуск мониторинга для пользователя 1259602460: период=7, коэф=1.5
🏬 Получаю список складов...
📊 Получаю коэффициенты для 30 складов...
✅ Получено 150 слотов, начинаю фильтрацию...
📅 После фильтрации по периоду (7 дней): 80 слотов
📊 После фильтрации по коэффициенту (<= 1.5): 42 слотов
```

## 🚀 Применение

```powershell
# Остановить бота
Get-Process | Where-Object {$_.ProcessName -eq "python"} | Stop-Process -Force

# Запустить с новым функционалом
cd wb_bot
python -m app.main
```

## ✅ Что работает

1. ✅ Выбор периода (7, 14, 30 дней, любая дата)
2. ✅ Выбор макс. коэффициента (1.0-3.0, любой)
3. ✅ Запрос к API WB (склады + коэффициенты)
4. ✅ Фильтрация по периоду (datetime)
5. ✅ Фильтрация по коэффициенту (числовое сравнение)
6. ✅ Группировка слотов по складам
7. ✅ Отображение результатов с деталями:
   - Общее количество слотов и складов
   - Название и ID каждого склада
   - Количество слотов на складе
   - Минимальный коэффициент
   - Ближайшие 3 даты с коэффициентами
8. ✅ Пагинация (5 складов на страницу)
9. ✅ Навигация (⬅️ Пред. / След. ➡️)
10. ✅ Обновление поиска (🔄)
11. ✅ Изменение параметров (⚙️)
12. ✅ Обработка ошибок (нет API ключа, ошибка API, нет результатов)

## 🎯 Преимущества

- **Точная фильтрация:** Показывает только склады с подходящими слотами
- **Детальная информация:** Минимальный коэффициент и даты для каждого склада
- **Удобная навигация:** Пагинация и кнопки управления
- **Актуальные данные:** Прямой запрос к API WB
- **Логирование:** Полная информация о процессе фильтрации

---

**Автомониторинг теперь работает с реальной фильтрацией по API!** 🎉



